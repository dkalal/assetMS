(function(){'use strict';const records=Array.from(document.querySelectorAll('[data-user-record]'));if(!records.length)return;const search=document.querySelector('[data-user-search]');const role=document.querySelector('[data-user-role-filter]');const status=document.querySelector('[data-user-status-filter]');const empty=document.querySelector('[data-user-no-results]');const announcement=document.querySelector('[data-user-results-status]');function filterRecords(){const query=(search?.value||'').trim().toLowerCase();const roleValue=role?.value||'';const statusValue=status?.value||'';let visible=0;records.forEach(record=>{const match=(!query||record.dataset.search.includes(query))&&(!roleValue||record.dataset.role===roleValue)&&(!statusValue||record.dataset.status===statusValue);record.hidden=!match;if(match)visible+=1;});empty?.classList.toggle('d-none',visible!==0);if(announcement)announcement.textContent=`${visible} user${visible===1?'':'s'} shown.`;}search?.addEventListener('input',filterRecords);role?.addEventListener('change',filterRecords);status?.addEventListener('change',filterRecords);})();
