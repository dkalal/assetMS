(function(){'use strict';var input=document.querySelector('.registration-card input[type="password"]');var bar=document.getElementById('passwordStrengthBar');var label=document.getElementById('passwordStrengthLabel');if(!input||!bar||!label)return;input.addEventListener('input',function(){var value=input.value;var score=0;if(value.length>=8)score++;if(value.length>=12)score++;if(/[a-z]/.test(value)&&/[A-Z]/.test(value))score++;if(/[0-9]/.test(value))score++;if(/[^A-Za-z0-9]/.test(value))score++;score=Math.min(score,4);var widths=[0,25,50,75,100];var labels=['','Weak','Fair','Good','Strong'];var classes=['','password-strength-weak','password-strength-fair','password-strength-good','password-strength-strong'];bar.style.width=widths[score]+'%';bar.className='password-strength-bar '+classes[score];label.textContent=labels[score];});}());
