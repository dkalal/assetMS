(function(){'use strict';document.addEventListener('click',function(event){var trigger=event.target.closest('[data-prefill-assignment]');if(!trigger)return;var form=document.getElementById('branch-manager-assignment');if(!form)return;var branch=form.querySelector('[name="branch"]');var manager=form.querySelector('[name="manager"]');if(branch)branch.value=trigger.dataset.branchId||'';if(manager)manager.value=trigger.dataset.managerId||'';form.scrollIntoView({behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'center'});if(branch)branch.focus({preventScroll:true});});}());
