(function(){
  'use strict';
  const root=document.querySelector('[data-settings-center]');if(!root)return;
  const profileForm=root.querySelector('[data-profile-form]');
  const profileAlert=root.querySelector('[data-profile-alert]');
  const show=(box,message,kind)=>{box.className=`alert alert-${kind}`;box.textContent=message;box.focus();};
  profileForm?.addEventListener('submit',async event=>{event.preventDefault();if(!profileForm.checkValidity()){profileForm.classList.add('was-validated');profileForm.reportValidity();return;}const button=profileForm.querySelector('[type=submit]');button.disabled=true;button.querySelector('.button-label').textContent='Saving…';try{const response=await fetch(profileForm.action,{method:'POST',body:new FormData(profileForm),headers:{'X-Requested-With':'XMLHttpRequest'},credentials:'same-origin'});const result=await response.json();if(!response.ok||!result.success)throw new Error(result.error||'Unable to save profile.');show(profileAlert,result.message||'Profile saved.','success');}catch(error){show(profileAlert,error.message,'danger');}finally{button.disabled=false;button.querySelector('.button-label').textContent='Save profile';}});
  root.querySelectorAll('[data-system-setting]').forEach(form=>form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('[type=submit]');const alertBox=root.querySelector('[data-system-alert]');button.disabled=true;button.querySelector('.button-label').textContent='Saving…';try{const response=await fetch('/settings/api/update/',{method:'POST',body:new FormData(form),headers:{'X-Requested-With':'XMLHttpRequest'},credentials:'same-origin'});const result=await response.json();if(!response.ok||!result.success)throw new Error(result.error||'Unable to save setting.');show(alertBox,result.message||'Setting saved.','success');}catch(error){show(alertBox,error.message,'danger');}finally{button.disabled=false;button.querySelector('.button-label').textContent='Save';}}));
})();
